---
import { Image } from "astro:assets";
import mor from "public/assets/morde.jpg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Image
  class="h-full w-full object-cover"
  src={mor}
  widths={widths}
  sizes={sizes}
  alt="My Maine Coon cat, Mor"
/>
